# ==============================================================================
# STREAMHUB ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control
#
# Required for: Development, Testing, Production
# Last updated: 2025-10-22
# ==============================================================================

# ------------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# ------------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Project Settings → API
# Format: https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon/Public key - Safe to expose in frontend
# Starts with: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# Service Role key - KEEP SECRET! Never expose in frontend
# Has admin privileges, bypasses RLS
# Starts with: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# ------------------------------------------------------------------------------
# Livepeer Configuration (REQUIRED for live streaming)
# ------------------------------------------------------------------------------
# Get API key from: https://livepeer.studio → Developers → API Keys
# Create a new key with streaming permissions
LIVEPEER_API_KEY=your-livepeer-api-key

# ------------------------------------------------------------------------------
# Application Configuration (REQUIRED in production)
# ------------------------------------------------------------------------------
# Your application's public URL (no trailing slash)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
# Options: development, production, test
NODE_ENV=development

# ------------------------------------------------------------------------------
# Testing Configuration (OPTIONAL - only for E2E tests)
# ------------------------------------------------------------------------------
# Create a test user in your Supabase project for automated testing
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# Playwright base URL (optional, defaults to NEXT_PUBLIC_APP_URL)
PLAYWRIGHT_BASE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Analytics & Monitoring (OPTIONAL)
# ------------------------------------------------------------------------------
# Google Analytics measurement ID
# Get from: https://analytics.google.com
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking
# Get from: https://sentry.io → Project Settings → Client Keys (DSN)
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# Sentry environment name
# SENTRY_ENVIRONMENT=production

# ------------------------------------------------------------------------------
# Feature Flags (OPTIONAL)
# ------------------------------------------------------------------------------
# Enable/disable features for testing or gradual rollout
# NEXT_PUBLIC_ENABLE_LIVE_STREAMING=true
# NEXT_PUBLIC_ENABLE_COMMENTS=true
# NEXT_PUBLIC_ENABLE_ANALYTICS=true
# NEXT_PUBLIC_ENABLE_AGE_VERIFICATION=true

# ------------------------------------------------------------------------------
# Storage Configuration (OPTIONAL - for custom CDN)
# ------------------------------------------------------------------------------
# Custom CDN URL for video delivery (if not using Supabase Storage directly)
# NEXT_PUBLIC_CDN_URL=https://cdn.your-domain.com

# Custom storage bucket names (if different from defaults)
# NEXT_PUBLIC_VIDEOS_BUCKET=videos
# NEXT_PUBLIC_THUMBNAILS_BUCKET=thumbnails
# NEXT_PUBLIC_AVATARS_BUCKET=avatars

# ------------------------------------------------------------------------------
# Email Configuration (OPTIONAL - for custom SMTP)
# ------------------------------------------------------------------------------
# If using custom SMTP instead of Supabase Auth emails
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM_EMAIL=noreply@your-domain.com
# SMTP_FROM_NAME=StreamHub

# ------------------------------------------------------------------------------
# Rate Limiting (OPTIONAL)
# ------------------------------------------------------------------------------
# Configure rate limits for API endpoints
# RATE_LIMIT_UPLOAD_PER_HOUR=10
# RATE_LIMIT_COMMENT_PER_MINUTE=5
# RATE_LIMIT_LIKE_PER_MINUTE=10

# ------------------------------------------------------------------------------
# Development Tools (OPTIONAL - dev only)
# ------------------------------------------------------------------------------
# Skip preflight checks in development
# SKIP_PREFLIGHT_CHECK=true

# Disable telemetry
# NEXT_TELEMETRY_DISABLED=1

# Enable debug mode
# DEBUG=true

# ------------------------------------------------------------------------------
# NOTES
# ------------------------------------------------------------------------------
# 1. NEXT_PUBLIC_* variables are exposed to the browser
# 2. Other variables are only available server-side
# 3. Restart dev server after changing environment variables
# 4. Use .env.local for local development (gitignored)
# 5. Use .env.production for production builds
# 6. Set secrets in your hosting platform's environment variables
# 7. Never commit real credentials to version control
#
# For more info: https://nextjs.org/docs/basic-features/environment-variables
